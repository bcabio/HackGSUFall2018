import * as React from 'react';
import { Col, Row } from 'react-bootstrap';

const Home: React.SFC<undefined> = () => {
  return (
    <Row>
      <Col>Welcome!</Col>
    </Row>
  );
};

export default Home;
